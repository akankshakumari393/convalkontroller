apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "convalkontroller.fullname" . }}-test-connection"
  labels:
    {{- include "convalkontroller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "convalkontroller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
