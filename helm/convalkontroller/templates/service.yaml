apiVersion: v1
kind: Service
metadata:
  name: {{ include "convalkontroller.fullname" . }}
  labels:
    {{- include "convalkontroller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "convalkontroller.selectorLabels" . | nindent 4 }}
